import React from 'react';
import { MainView } from '../MainView';

import './App.scss';

export const App = () => {
  return (
    <div className="app app-background">
      <MainView />
    </div>
  );
};
